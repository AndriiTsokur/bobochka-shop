import { Container } from '@/components/shared';

export const metadata = {
	title: 'New Arrivals',
};

export default function NewArrivals() {
	return (
		<Container>
			<h1>New Arrivals</h1>
		</Container>
	);
}
